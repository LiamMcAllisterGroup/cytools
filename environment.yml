name: cytools
channels:
  - conda-forge
dependencies:
  # CYTools dependencies
  - python<3.13
  - numba
  - numpy
  - normaliz
  - scipy
  - cvxopt
  - python-flint
  - pplpy
  - joblib
  - tqdm
  - requests
  - osqp
  - pip
  - pip:
    - ortools
    - mosek
    - triangulumancer
    - pypalp
    - qpsolvers
    - cygv
    # The actual cytools package (taken from pypi)
    - cytools
  # Common useful tools
  - jupyterlab
  - matplotlib
  - plotly
  - ipywidgets
  - anywidget
  # Uncomment the following lines for additional tools
  # - sage